@model IEnumerable<CSGO.Models.team>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.wins)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.defeats)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.match.map)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.player.username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rating.id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.wins)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.defeats)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.match.map)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.player.username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.rating.id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
